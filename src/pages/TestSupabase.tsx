const TestSupabase = () => {
  return (
    <div className="p-6">
      <h1 className="text-xl font-bold mb-4">Supabase test disabled</h1>
      <p className="text-muted-foreground">This project now uses Firebase. This diagnostic page was kept to avoid broken links.</p>
    </div>
  );
};

export default TestSupabase;
